<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.android.countrycodegenerator.MainActivity"
    tools:showIn="@layout/activity_main">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:text="-Type in the two letter country code (ISO alpha-2) to generate corresponding country name\n-Type in the country name to generate corresponding two letter country code (ISO alpha-2)"
        android:textSize="14sp"
        android:textStyle="bold" />

    <EditText
        android:id="@+id/txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginStart="72dp"
        android:layout_marginTop="103dp"
        android:ems="10"
        android:hint="Type Here"
        android:inputType="text|textCapCharacters|textCapWords|textCapSentences"
        android:selectAllOnFocus="false"
        android:singleLine="false"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/result"
        android:layout_width="300dp"
        android:layout_height="30dp"
        android:layout_below="@+id/txt"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="115dp"
        android:textAlignment="center"
        android:textAppearance="@style/TextAppearance.AppCompat.Button"
        android:textColor="@android:color/holo_red_dark"
        android:textStyle="bold" />

    <Button
        android:id="@+id/button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/txt"
        android:layout_below="@+id/txt"
        android:layout_marginEnd="58dp"
        android:layout_marginTop="14dp"
        android:onClick="iso"
        android:text="Generate" />
</RelativeLayout>
